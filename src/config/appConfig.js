export const DATE_FORMAT = {
  DD_MM_YYYY_HH_MM_SS: 'MMM dd yyyy',
  YYYY_MM_DD_HH_MM_SS: 'yyyy-MM-dd HH:mm:ss',
};

export const REGEXP = {
  decimalValues: /^\d*\.?\d*$/,
  onlyNumber: /^([0-9]*)$/,
};
